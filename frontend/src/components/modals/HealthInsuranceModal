import React from "react";
import { FaTimes, FaFileAlt, FaHeartbeat } from "react-icons/fa";

export default function HealthInsuranceModal({ onClose }) {
  return (
    <div className="fixed inset-0 z-50 flex items-start justify-center p-6">
      <div className="absolute inset-0 bg-black/50" onClick={onClose}></div>

      <div className="relative bg-white max-w-4xl w-full rounded-3xl shadow-2xl overflow-y-auto max-h-[90vh]">
        <div className="flex items-center justify-between p-5 border-b bg-blue-50">
          <h3 className="text-2xl font-bold text-blue-700 flex items-center gap-2">
            <FaHeartbeat className="text-red-500" /> Health Insurance — Full
            Details
          </h3>
          <button
            onClick={onClose}
            className="p-2 hover:bg-gray-100 rounded-full"
          >
            <FaTimes />
          </button>
        </div>

        <div className="p-6 space-y-6">
          <h4 className="text-xl font-semibold text-blue-700 flex items-center gap-2">
            <FaFileAlt className="text-blue-600" /> Overview
          </h4>

          <p>
            Health Insurance protects you against medical expenses, hospital
            bills, surgery costs, and critical illness treatments.
          </p>

          {/* you can copy sections from your life modal or add more */}
        </div>
      </div>
    </div>
  );
}
