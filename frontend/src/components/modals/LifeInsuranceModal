import React from "react";
import {
  FaTimes,
  FaShieldAlt,
  FaFileAlt,
  FaCalculator,
  FaCheckCircle,
} from "react-icons/fa";
export default function LifeInsuranceModal({ onClose }) {
  return (
    <div className="fixed inset-0 z-50 flex items-start justify-center p-6">
      {/* Overlay */}
      <div
        className="absolute inset-0 bg-black/50 backdrop-blur-sm"
        onClick="{onClose}"
      ></div>

      {/* Modal Container */}
      <div className="relative bg-white max-w-4xl w-full rounded-3xl shadow-2xl overflow-y-auto max-h-[92vh]">
        {/* Header */}
        <div className="flex items-center justify-between p-5 border-b bg-blue-50 rounded-t-3xl">
          <h3 className="text-2xl font-bold text-blue-700 flex items-center gap-3">
            <FaShieldAlt className="text-blue-600" />
            Life Insurance — Complete Details
          </h3>

          <button
            className="p-2 hover:bg-gray-100 rounded-full transition"
            onClick="{onClose}"
          >
            <FaTimes size="{20}" />
          </button>
        </div>

        {/* Body */}
        <div className="p-6 space-y-8">
          {/* Overview Section */}
          <section>
            <h4 className="text-xl font-semibold text-blue-700 flex items-center gap-2">
              <FaFileAlt className="text-blue-600" /> What is Life Insurance?
            </h4>

            <p className="mt-2 text-gray-700 leading-relaxed">
              Life Insurance provides financial protection to your family in
              case of unexpected events. It ensures your loved ones remain
              financially stable through a lump-sum payout, systematic income,
              or wealth-building policy benefits.
            </p>
          </section>

          {/* Types of Life Insurance */}
          <section>
            <h4 className="text-xl font-semibold text-blue-700 mb-3">
              Types of Life Insurance
            </h4>

            <ul className="space-y-3">
              <li className="flex items-start gap-3">
                <FaCheckCircle className="text-green-600 mt-1" />
                <p>
                  <b>Term Life Insurance</b> – High coverage at low premium.
                </p>
              </li>

              <li className="flex items-start gap-3">
                <FaCheckCircle className="text-green-600 mt-1" />
                <p>
                  <b>Whole Life Insurance</b> – Lifetime protection + guaranteed
                  benefits.
                </p>
              </li>

              <li className="flex items-start gap-3">
                <FaCheckCircle className="text-green-600 mt-1" />
                <p>
                  <b>ULIP</b> – Insurance + market-linked investment returns.
                </p>
              </li>

              <li className="flex items-start gap-3">
                <FaCheckCircle className="text-green-600 mt-1" />
                <p>
                  <b>Endowment Plans</b> – Savings + life cover with maturity
                  payout.
                </p>
              </li>

              <li className="flex items-start gap-3">
                <FaCheckCircle className="text-green-600 mt-1" />
                <p>
                  <b>Child Plans</b> – Future financial security for your
                  child's education.
                </p>
              </li>
            </ul>
          </section>

          {/* Calculator Section */}
          <section>
            <h4 className="text-xl font-semibold text-blue-700 mb-4 flex items-center gap-2">
              <FaCalculator className="text-blue-600" /> Premium Calculator
            </h4>

            {/* Calculator UI */}
            <div className="grid sm:grid-cols-2 gap-5 bg-blue-50 p-5 rounded-xl">
              <div className="flex flex-col">
                <label className="text-sm font-semibold text-gray-700">
                  Age
                </label>
                <input
                  type="number"
                  className="p-2 border rounded-lg mt-1"
                  placeholder="Enter your age"
                />
              </div>

              <div className="flex flex-col">
                <label className="text-sm font-semibold text-gray-700">
                  Sum Assured (₹)
                </label>
                <input
                  type="number"
                  className="p-2 border rounded-lg mt-1"
                  placeholder="E.g., 50,00,000"
                />
              </div>

              <div className="flex flex-col">
                <label className="text-sm font-semibold text-gray-700">
                  Policy Term (Years)
                </label>
                <input
                  type="number"
                  className="p-2 border rounded-lg mt-1"
                  placeholder="E.g., 20"
                />
              </div>

              <div className="flex flex-col">
                <label className="text-sm font-semibold text-gray-700">
                  Smoker?
                </label>
                <select className="p-2 border rounded-lg mt-1">
                  <option>No</option>
                  <option>Yes</option>
                </select>
              </div>
            </div>

            {/* CTA */}
            <div className="mt-5">
              <button className="bg-blue-600 text-white px-6 py-2 rounded-lg shadow hover:bg-blue-700 transition">
                Calculate Premium
              </button>
            </div>
          </section>

          {/* Benefits */}
          <section>
            <h4 className="text-xl font-semibold text-blue-700 mb-3">
              Key Benefits
            </h4>

            <ul className="space-y-3">
              <li className="flex items-start gap-3">
                <FaCheckCircle className="text-green-600 mt-1" /> Financial
                protection for family
              </li>
              <li className="flex items-start gap-3">
                <FaCheckCircle className="text-green-600 mt-1" /> Tax benefits
                under Section 80C & 10(10D)
              </li>
              <li className="flex items-start gap-3">
                <FaCheckCircle className="text-green-600 mt-1" /> Affordable
                premium options
              </li>
              <li className="flex items-start gap-3">
                <FaCheckCircle className="text-green-600 mt-1" /> Long-term
                wealth creation
              </li>
            </ul>
          </section>

          {/* Final CTA */}
          <div className="mt-8 text-center">
            <button className="bg-green-600 text-white px-8 py-3 rounded-xl shadow hover:bg-green-700 transition">
              Get a Life Insurance Quote
            </button>
          </div>
        </div>
      </div>
    </div>
  );
}
